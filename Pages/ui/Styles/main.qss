/* ── GLOBAL ── */
QMainWindow { background: @WINDOW_BG@; }
QScrollArea { background: transparent; border: none; }
QWidget { font-family: '@FONT_TITLE@'; }

/* ── LAYOUT ELEMENTS ── */
#sidebar {
    background: @SIDEBAR_BG@;
    border-right: 1px solid @SIDEBAR_BORDER@;
}
#logoArea {
    background: @SIDEBAR_BG@;
    border-bottom: 1px solid @SIDEBAR_BORDER@;
}
#osintDashboard {
    background: @WINDOW_BG@;
}
#placeholderPage {
    background: @WINDOW_BG@;
}

/* ── REUSABLE CARD ── */
Card {
    background: @CARD_BG@;
    border: 1px solid @CARD_BORDER@;
    border-radius: 12px;
}

/* ── GLOWING INPUT FIELD ── */
GlowInput {
    background: @INPUT_BG@;
    border: 1px solid @INPUT_BORDER@;
    border-radius: 8px;
    color: @TEXT_BODY@;
    padding: 0 14px;
    selection-background-color: @INPUT_SELECTION@;
}
GlowInput:focus {
    border: 1px solid @INPUT_FOCUS@;
}
GlowInput::placeholder {
    color: @TEXT_PLACEHOLDER@;
}

/* ── Glowing BUTTON ── */
GlowingButton {
    border-radius: 8px;
    padding: 0 20px;
    letter-spacing: 1px;
}

/* Primary Variant */
GlowingButton[variant="primary"] {
    background: @BTN_PRIMARY_BG@;
    border: 1px solid @BTN_PRIMARY_BORDER@;
    color: @BTN_PRIMARY_TEXT@;
}
GlowingButton[variant="primary"]:hover { background: @BTN_PRIMARY_HOVER@; }
GlowingButton[variant="primary"]:pressed { background: @BTN_PRIMARY_PRESS@; }

/* Danger Variant */
GlowingButton[variant="danger"] {
    background: @BTN_DANGER_BG@;
    border: 1px solid @BTN_DANGER_BORDER@;
    color: @BTN_DANGER_TEXT@;
}
GlowingButton[variant="danger"]:hover { background: @BTN_DANGER_HOVER@; }
GlowingButton[variant="danger"]:pressed { background: @BTN_DANGER_PRESS@; }

/* ── SIDEBAR NAV BUTTON ── */
NavButton {
    background: transparent;
    border: none;
    border-left: 3px solid transparent;
    border-radius: 0px;
    color: @NAV_TEXT_IDLE@;
    text-align: left;
    padding-left: 16px;
    font-size: 14px;
}
NavButton:hover {
    background: @NAV_BG_HOVER@;
    color: @NAV_TEXT_HOVER@;
}
NavButton:checked {
    background: @NAV_BG_ACTIVE@;
    border-left: 3px solid @NAV_BORDER_ACTIVE@;
    color: @NAV_TEXT_ACTIVE@;
    font-weight: bold;
}

/* ── RESULT DISPLAY ── */
ResultDisplay {
    background: @CARD_BG@;
    border: 1px solid @CARD_BORDER@;
    border-radius: 10px;
    color: @TEXT_TERMINAL@;
    padding: 14px;
    selection-background-color: @INPUT_SELECTION@;
}
ResultDisplay QScrollBar:vertical {
    background: @SCROLLBAR_BG@;
    width: 6px;
    border-radius: 3px;
}
ResultDisplay QScrollBar::handle:vertical {
    background: @SCROLLBAR_HANDLE@;
    border-radius: 3px;
}



/* HYPER BUTTON */
HyperButton[variant="primary"] {
        color: @NAV_TEXT_IDLE@;
        background-color: transparent;

        border: none;
        text-decoration: underline;
        text-align: left;
}

HyperButton[variant="primary"]:hover { background: transparent; color: @TEXT_BODY@ }
HyperButton[variant="primary"]:pressed { background: transparent; }



#networkPage {
    background: @WINDOW_BG@;
}